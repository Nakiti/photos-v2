import React from 'react';
import { View, StyleSheet } from 'react-native';

const ShareEventScreen = () => {
  return (
    <View style={styles.container}>
      {/* Share event screen content */}
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default ShareEventScreen;
