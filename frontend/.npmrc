# React 19 + some RN libs (e.g. react-native-fast-image) still declare React 17/18
# peer ranges, so a strict install errors with ERESOLVE. The project intentionally
# runs React 19; install with legacy peer resolution.
legacy-peer-deps=true
